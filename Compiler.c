# Compiler for the C# Runtime
# A Project that is an alternative and is a faster version of C# Libraries on Linux

#include <stdlib.h>
#include <stdio.h>
#include <sys.h>

main(){

f.open(compilerfile);

f.read(compilerfiles);

f.read(compilefile);

}
